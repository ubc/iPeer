<?php $insPos=1;

  if (!empty($courseInstructors)) {
    
    //foreach($courseInstructors as $row){
    foreach($courseInstructors as $row){
print_r($row);
      
      isset($row['UserCourse'])? $instructorId = $row['UserCourse']['id'] : $instructor =null;
      if (isset($instructor) && $instructor['id']!=1) {
              $params = array('controller'=>$controller, 'userId'=>$instructor['id']);
              echo $this->renderElement('users/user_info', $params);
  
              if ($insPos > 0 && $insPos < count($courseInstructors)-1) {
                echo "; ";
              }
              $insPos++;
            }
     } 
    }   
   
   if ($insPos == 1) {
     echo 'N/A'; 
   }

   ?>